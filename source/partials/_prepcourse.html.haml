.row
  .large-4.columns
  -data.prep.each_slice(3) do |group|
    .row
    - group.each do |item|
      .small-6.large-4.columns
        %h5= link_to item[:name], item[:url], target: '_blank'
        %p= item[:description]
